//Prompt the user for the max limit of loops

let maxlimit = parseInt(prompt("Enter the limit of the Double Loops:));

for (let i = 0; i <= maxLimit; i++) {
  for (let j = 0; j <= maxtimit; j++) (
    let addedValue = i + j;
    console.log('[${i}] Added value is ${addedValue}');
 }
}
